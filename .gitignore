node_modules/
dist/
dist-portal/
dist-cdn/
.DS_Store
*.log
.idea/
.vscode/
coverage/
.released-version
junit-report.xml

# work with claude agents — team-shared config (.claude/ commands, agents,
# handbooks, templates, memory, settings.json, and .mcp.json) is tracked;
# only per-user state stays local
.claude/settings.local.json
/todo

# Auto-generated by the desktop app; the working dev-server config lives
# at the ClaudeSandbox root, not here.
.claude/launch.json
